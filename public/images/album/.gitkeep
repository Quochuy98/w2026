# Thư mục lưu trữ ảnh cưới cục bộ
# Bạn có thể copy trực tiếp các file ảnh (.jpg, .jpeg, .png, .webp, .avif) vào thư mục này.
# Trang web sẽ tự động quét và hiển thị tất cả ảnh trong thư mục này lên Album ảnh cưới.
